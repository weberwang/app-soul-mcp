# This file is only needed when running the server directly (outside VS Code).
# When using VS Code MCP, keys are configured in .vscode/mcp.json via inputs.

UNSPLASH_ACCESS_KEY=
OUTPUT_DIR=./output

